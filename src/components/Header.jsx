import { motion } from "framer-motion";

export const Header = () => {
    return (
        <motion.div className="top-banner">
            <motion.p className="title">Wordle.copy</motion.p>
        </motion.div>
    );
}